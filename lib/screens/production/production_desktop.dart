import 'package:flutter/material.dart';

class ProductionDesktopScreen extends StatelessWidget {
  const ProductionDesktopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
