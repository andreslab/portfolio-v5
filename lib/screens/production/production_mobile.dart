import 'package:flutter/material.dart';

class ProductionMobileScreen extends StatelessWidget {
  const ProductionMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
