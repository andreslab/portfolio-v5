import 'package:flutter/material.dart';

class ProductionTabletScreen extends StatelessWidget {
  const ProductionTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
