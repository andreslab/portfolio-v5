import 'package:flutter/material.dart';

class FinancesTabletScreen extends StatelessWidget {
  const FinancesTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
